对方要搜本子（关键词 / 标签 / 标题）→ 调 jmcomic_search（mode=tag 或 title，keyword 取对方原话里的词）。
对方给出 5 位以上数字 ID，或说「看/下/要」某 ID → 调 jmcomic_download，comic_id 用该数字。
预览卡或 PDF 由工具发出：ok=true 只表示命令跑通；delivered=true 才表示预览/文件已到聊天。
「下载任务已启动」≠ 已发出——delivered=false 时别说「发给你了」。
当前：{jm_hint}
